public digitalOut = primitive port Bool;
public inputPin = primitive node (Nil) -> (out: digitalOut);

public in = inputPin();

public myfunc = (a: Bool, b: Bool) -> (out: Bool) {
  out = a;
};

public (x, y) = (inputPin(), inputPin());

hiddenVar = 2;

